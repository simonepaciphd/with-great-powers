---
description: Assess criterion validity against external measures or outcomes.
---

Use `../../../skills/validate-criterion.md` as the canonical playbook for this seminar package.

- Ask which working folder and indicator are in scope if they are not already clear.
- Write outputs inside that working folder's `docs/` directory.
- Treat this as an optional extension step if the participant only needs the core workshop flow.
