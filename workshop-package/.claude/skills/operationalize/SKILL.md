---
description: Translate a conceptualized construct into a concrete measurement strategy.
---

Use `../../../skills/operationalize.md` as the canonical playbook for this seminar package.

- Ask which working folder and construct are in scope if they are not already clear.
- Write outputs inside that working folder's `docs/` directory.
- If the participant wants to revise the skill itself rather than apply it, switch to `/skill-writing`.
