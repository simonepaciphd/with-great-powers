---
description: Stress-test whether an indicator captures the intended construct.
---

Use `../../../skills/validate-construct.md` as the canonical playbook for this seminar package.

- Ask which working folder and indicator are in scope if they are not already clear.
- Write outputs inside that working folder's `docs/` directory.
- Keep the package self-contained; do not rely on external skill libraries.
