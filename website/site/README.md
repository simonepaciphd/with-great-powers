# Website Rebuild Staging

Use this folder for a future site rebuild that should be kept separate from the current GitHub Pages deployment under `docs/`.

For now, this is only a staging area. No production routing or hosting config has been changed.
